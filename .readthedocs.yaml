version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.7"
    
sphinx:
   configuration: docs/conf.py
   
python:
  # Install our python package before building the docs
  install:
    - method: pip
      path: .
